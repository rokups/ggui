diff --git a/TextDiff.cpp b/TextDiff.cpp
index d308483..038ee64 100644
--- a/TextDiff.cpp
+++ b/TextDiff.cpp
@@ -49,5 +48,0 @@ void TextDiff::SetText(const std::string_view& left, const std::string_view& rig
-	// setup line number decoration
-	leftLineNumberDigits = static_cast<int>(std::log10(leftDocument.lineCount() + 1) + 1.0f);
-	rightLineNumberDigits = static_cast<int>(std::log10(leftDocument.lineCount() + 1) + 1.0f);
-	decoratorWidth = -(leftLineNumberDigits + rightLineNumberDigits + 4.0f);
-
@@ -83,0 +79,2 @@ void TextDiff::SetText(const std::string_view& left, const std::string_view& rig
+	SetLineNumbers({});
+
@@ -88,0 +86,21 @@ void TextDiff::SetText(const std::string_view& left, const std::string_view& rig
+//
+//	TextDiff::SetLineNumbers
+//
+
+void TextDiff::SetLineNumbers(const std::vector<std::pair<int, int>>& numbers) {
+	lineNumbers = numbers.size() == lineInfo.size() ? numbers : std::vector<std::pair<int, int>>{};
+	int maximumLeft = leftDocument.lineCount();
+	int maximumRight = rightDocument.lineCount();
+	if (!lineNumbers.empty()) {
+		maximumLeft = maximumRight = 1;
+		for (const auto& [left, right] : lineNumbers) {
+			maximumLeft = std::max(maximumLeft, left);
+			maximumRight = std::max(maximumRight, right);
+		}
+	}
+	leftLineNumberDigits = static_cast<int>(std::to_string(maximumLeft).size());
+	rightLineNumberDigits = static_cast<int>(std::to_string(maximumRight).size());
+	decoratorWidth = -(leftLineNumberDigits + rightLineNumberDigits + 4.0f);
+}
+
+
@@ -179,2 +197 @@ void TextDiff::decorateLine(TextEditor::Decorator& decorator) {
-	auto lineLeft = static_cast<int>(line.leftLine + 1);
-	auto lineRight = static_cast<int>(line.rightLine + 1);
+	auto [lineLeft, lineRight] = getLineNumbers(static_cast<size_t>(decorator.line));
@@ -181,0 +199,3 @@ void TextDiff::decorateLine(TextEditor::Decorator& decorator) {
+	auto left = lineLeft > 0 ? std::to_string(lineLeft) : "";
+	auto right = lineRight > 0 ? std::to_string(lineRight) : "";
+	auto marker = line.status == LineStatus::added ? '+' : line.status == LineStatus::deleted ? '-' : ' ';
@@ -183,4 +203,3 @@ void TextDiff::decorateLine(TextEditor::Decorator& decorator) {
-	switch(line.status) {
-		case LineStatus::common:
-			ImGui::TextColored(color, " %*d %*d  ", leftLineNumberDigits, lineLeft, rightLineNumberDigits, lineRight);
-			break;
+	ImGui::TextColored(
+		color, " %*s %*s %c", leftLineNumberDigits, left.c_str(), rightLineNumberDigits, right.c_str(), marker);
+}
@@ -188,3 +206,0 @@ void TextDiff::decorateLine(TextEditor::Decorator& decorator) {
-		case LineStatus::added:
-			ImGui::TextColored(color, " %*s %*d +", leftLineNumberDigits, "", rightLineNumberDigits, lineRight);
-			break;
@@ -192,3 +208,7 @@ void TextDiff::decorateLine(TextEditor::Decorator& decorator) {
-		case LineStatus::deleted:
-			ImGui::TextColored(color, " %*d %*s -", leftLineNumberDigits, lineLeft, rightLineNumberDigits, "");
-			break;
+//
+//	TextDiff::getLineNumbers
+//
+
+std::pair<int, int> TextDiff::getLineNumbers(size_t index) const {
+	if (lineNumbers.size() == lineInfo.size()) {
+		return lineNumbers[index];
@@ -195,0 +216,6 @@ void TextDiff::decorateLine(TextEditor::Decorator& decorator) {
+
+	const auto& line = lineInfo[index];
+	return {
+		line.status == LineStatus::added ? 0 : static_cast<int>(line.leftLine + 1),
+		line.status == LineStatus::deleted ? 0 : static_cast<int>(line.rightLine + 1)
+	};
@@ -294,2 +320,3 @@ void TextDiff::renderSideBySideBackground() {
-		auto lineLeft = static_cast<int>(line.leftLine + 1);
-		auto lineRight = static_cast<int>(line.rightLine + 1);
+		auto [lineLeft, lineRight] = getLineNumbers(static_cast<size_t>(i));
+		auto left = lineLeft > 0 ? std::to_string(lineLeft) : "";
+		auto right = lineRight > 0 ? std::to_string(lineRight) : "";
@@ -299 +326 @@ void TextDiff::renderSideBySideBackground() {
-				snprintf(buffer, sizeof(buffer), " %*d", leftLineNumberDigits, lineLeft);
+				snprintf(buffer, sizeof(buffer), " %*s", leftLineNumberDigits, left.c_str());
@@ -301 +328 @@ void TextDiff::renderSideBySideBackground() {
-				snprintf(buffer, sizeof(buffer), " %*d", rightLineNumberDigits, lineRight);
+				snprintf(buffer, sizeof(buffer), " %*s", rightLineNumberDigits, right.c_str());
@@ -306 +333 @@ void TextDiff::renderSideBySideBackground() {
-				snprintf(buffer, sizeof(buffer), " %*d +", rightLineNumberDigits, lineRight);
+				snprintf(buffer, sizeof(buffer), " %*s +", rightLineNumberDigits, right.c_str());
@@ -317 +344 @@ void TextDiff::renderSideBySideBackground() {
-				snprintf(buffer, sizeof(buffer), " %*d -", leftLineNumberDigits, lineLeft);
+				snprintf(buffer, sizeof(buffer), " %*s -", leftLineNumberDigits, left.c_str());
diff --git a/TextDiff.h b/TextDiff.h
index 454714e..03e4d87 100644
--- a/TextDiff.h
+++ b/TextDiff.h
@@ -10 +9,0 @@
-
@@ -14,0 +14,2 @@
+#include <utility>
+
@@ -32,0 +34 @@ public:
+	void SetLineNumbers(const std::vector<std::pair<int, int>>& numbers);
@@ -89,0 +92 @@ private:
+	std::vector<std::pair<int, int>> lineNumbers;
@@ -114,0 +118 @@ private:
+	std::pair<int, int> getLineNumbers(size_t line) const;
